#creating graphs and charts in python 
#data used is Titanic .csv

import pandas as pd
df=pd.read_csv("https://calmcode.io/static/data/titanic.csv")
df.head(3)

#data visualisation 
import matplotlib.pyplot as plt
k=df["sex"].value_counts()
k
#display the graph , colur can be chaged according to the wish .
x=k.index
y=k.values
 
plt.bar(x,y,color="Black")
plt.show


#HISTOGRAM
plt.hist(df["age"],color="Lavender",edgecolor="Black")
plt.xlabel("Age")
plt.ylabel("Frequency");
plt.title("Age Distribution")
plt.show()

#data processing 
X = df.drop(['name','survived',"fare"], axis=1)
y=df['survived']
display(X.head(3))

from sklearn.preprocessing import LabelEncoder
#ML only take inputs as numbers so that numbers is labelencoders 
le=LabelEncoder()
X['sex']=le.fit_transform(X['sex'])
display(X.head())

from sklearn.preprocessing import StandardScaler
scaler=StandardScaler()
X_scaled = X[['age']].copy()
X_scaled
